const num1 = 5;
const num2 = 10;

function addValues() {
    console.log(`the sum is ${num1 + num2}`);
}

//daca avem functii care se executa o sa se vada rezultatul atunci cand executam fisierul in care sunt importate
addValues();